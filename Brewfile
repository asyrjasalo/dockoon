# Install with `brew bundle` (https://github.com/Homebrew/homebrew-bundle)

# Casks for OS X, see https://mockoon.com/#download for other OSes
cask 'mockoon'

# For .pre-commit-config.yaml
brew 'shellcheck'
